/**
 * Barrel for QuestionBankPage internals. The page imports via `@/teacher/qbank`.
 * Files inside this folder import siblings via direct relative paths (no cycles).
 */
export * from "./catalog";
