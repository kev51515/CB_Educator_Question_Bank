{
  "name": "cb_educator_question_bank",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:export-sets": "node --test scripts/export-sets/__tests__/*.test.mjs",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "build:exports": "node scripts/export-sets/build-sets.mjs --all && node scripts/export-sets/build-index.mjs",
    "build:exports:env": "set -a && [ -f viewer/.env.local ] && source viewer/.env.local; set +a; npm run build:exports"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "puppeteer": "^24.34.0"
  },
  "devDependencies": {
    "@playwright/test": "1.60.0"
  }
}
